# Backlog — отложенный полишинг

Мелкие, **неблокирующие** правки, которые копим и закрываем **одним скоупом в полишинг-спринт**. Сознательно не тащим их поштучно в текущие вехи — это держит фокус и хирургичность правок (CLAUDE.md rule 2/3).

**Статус: M5 «Экспорт и полировка» прошёл (2026-06-26)** — полишинг-спринт закрыл почти весь накопленный список (см. «Сделано»). Открытым осталось то, что план M5 намеренно отложил, плюс пара известных-принятых миноров из финального ревью M5.

Формат: `[ ]` открыто / `[x]` сделано. Поля: область · что · почему отложено · оценка · откуда.

## Открытые

- [ ] **`updateWidgetTitle` без UI-вызова.** Действие в сторе реализовано и юнит-тестировано (`session.ts`, часть document-model API), но заголовок виджета в UI статичен (`<span className="widget-title">`, в отличие от editable подписи). План M5 намеренно отложил click-to-edit заголовка в бэклог (не вошло в скоуп вехи). Либо повесить click-to-edit на `.widget-title` (зеркало подписи/`TextBlockView`), либо оставить как API-only с комментарием. Оценка: S. Источник: M4 финальный review → отложено планом M5.

### Известные-принятые (минор, из финального review M5 — не баги, фиксируем чтоб не потерять)

- [ ] **`key={block.sql}` на `WidgetBlockView` инертен в Отчёте.** В `Report.tsx` виджет получает `key={block.sql}` для ремоунта-в-loading при смене SQL, но SQL виджета в Отчёте иммутабелен (фиксируется при пине) → механизм сейчас не срабатывает. Это осознанный safety-net на будущее (editable widget SQL в v1.5+); регидрацию не ломает (re-run идёт по `loadedKey` в эффекте). Альтернатива для чистоты — `key={block.id}`. Оценка: XS, косметика. Источник: M5 финальный review (code-quality lens).

- [ ] **Экспорт: chart-виджет без числовой колонки → рисуется таблицей.** Если у виджета `vizType==='chart'`, но пересчитанный результат не даёт числовой колонки (`buildChartSpec → null`), экспорт показывает таблицу, тогда как живой `WidgetBlockView` рисует пометку «нет числовой колонки для графика». Расхождение live↔export; таблица — разумный фоллбэк, оставлено намеренно. Решить при желании паритета: нести ту же пометку в экспорт. Оценка: XS. Источник: M5 финальный review (integration + code-quality lenses).

## Сделано

### M5 «Экспорт и полировка» (2026-06-26, полишинг-спринт)

- [x] **Dev-консоль: sourcemap-шум DuckDB-WASM** — `customLogger` в `vite.config.ts` глушит ровно строки воркеров DuckDB (`015ba60`).
- [x] **Build chunk-size advisory** — поднят `build.chunkSizeWarningLimit` (`015ba60`).
- [x] **Lint-warning `useVirtualizer`** — точечный `eslint-disable react-hooks/incompatible-library` с обоснованием в `ResultGrid.tsx`; lint теперь 0 errors / 0 warnings (`015ba60`).
- [x] **Неотслеживаемые фикстуры** — `fixtures/acceptance.sql` + `events.csv` закоммичены (`204d582`); `metrics.parquet` — с `!fixtures/metrics.parquet` исключением в `.gitignore`, т.к. `acceptance.sql` на неё ссылается (`5032702`).
- [x] **Метка «применить к колонке» → «применить»** — релейбл в `SchemaColumnEditor` (`42edc89`).
- [x] **Показывать `STRING` вместо `VARCHAR`** — UI-алиас в пикере типов и в схеме рейла; физический тип/DDL остаются VARCHAR (`42edc89`).
- [x] **Мёртвое поле `Dataset.dirty`** — поле + все записи удалены, тесты поправлены (`6f1deb3`).
- [x] **`TextBlockView` пишет на blur всегда** — теперь пишет только при `draft !== block.markdown` (`953fc6f`).
- [x] **Пустой «источник(и):» в ошибке виджета** — при `datasetNames.length === 0` показывается только текст ошибки (`953fc6f`).
- [x] **Рейл в Отчёте при активном текст-блоке** — гасится в Отчёте без активного виджета (`mode==='report'` без widget → `null`); Explore не затронут (`953fc6f`). Приёмка подтвердила — ок.
- [x] **`WidgetBlockView` — сброс в `loading` при смене SQL** — через `key={block.sql}` ремоунт (без синхронного `setState` в эффекте); см. известный-минор выше про инертность в Отчёте (`953fc6f`).
- [x] **Редактирование названий табов** — double-click → inline-инпут; `renameTab(id,title)` (TDD); Enter/blur сохраняет, Esc отменяет (`7a4a693`, `3b8f2c4`).
- [x] **Табы — Sublime-стиль** — перерисован таб-стрип (форма вкладок, выраженный активный, close на ховере, `.tab-rename`) (`36a6e51`); `.tab-title { min-width:0 }` для эллипсиса (`ff420d7`).

### Ранее

- [x] **Dropzone — в рейл, не на всю ширину** (2026-06-26, M4-полиш). Перенесена в рейл над «Источники», компактная; доступна в обоих режимах (удобно для регидрации). (`db21939`)
- [x] **Семантика «сброс»/«сбросить» — унифицировано** (2026-06-23). Оба возвращают к VARCHAR-бейзлайну (`resetColumn` чинён через TDD; per-column «сбросить» = хедерная «сброс» на одну колонку).
