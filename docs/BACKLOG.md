# Backlog — отложенный полишинг

Мелкие, **неблокирующие** правки, которые копим и закрываем **одним скоупом в полишинг-спринт**. Сознательно не тащим их поштучно в текущие вехи — это держит фокус и хирургичность правок (CLAUDE.md rule 2/3).

**Статус: M5 «Экспорт и полировка» прошёл (2026-06-26)** — полишинг-спринт закрыл почти весь накопленный список (см. «Сделано»). Открытым осталось то, что план M5 намеренно отложил, плюс пара известных-принятых миноров из финального ревью M5.

Формат: `[ ]` открыто / `[x]` сделано. Поля: область · что · почему отложено · оценка · откуда.

## Открытые

- [x] **`updateWidgetTitle` без UI-вызова.** Закрыто: click-to-edit заголовка реализован в M7b (WidgetBlockView editingTitle).

### UX-полиш из приёмки M9 (2026-07-06)

- [x] **Дот-команды не задокументированы в «?» (AboutModal).** `.tables` / `.schema <t>` / `.help` сейчас открываются только через сам `.help`; в About-модалке про REPL ни слова. Добавить короткий блок «терминал»: дот-команды + история ↑/↓ (курсор на первой/последней строке). Оценка: XS. Источник: приёмка M9 (прямой запрос юзера).
- [x] **История запросов невидима в UI — счётчик и кнопки ↑/↓ рядом с «запустить».** Фича доступна только с клавиатуры и только если знаешь про курсор на первой/последней строке. Рядом с кнопкой запуска — компактный индикатор истории (например `3/17`) и кликабельные ↑/↓, шагающие по истории тем же механизмом, что стрелки в редакторе. Дискаверабилити + мышиный путь. Оценка: S. Источник: приёмка M9 (прямой запрос юзера).

### Известные-принятые (минор, из финального review M7b «Исполняемые ячейки» 2026-07-06 — не блокеры)

- [ ] **`extractDatasetNames` матчит любой идентификатор, не только таблицы.** Датасет с именем, совпадающим с колонкой/алиасом/CTE в SQL, даёт ложный источник (таблица `id` + `SELECT id FROM orders` → `datasetNames: ['id']`) → лишний source-pill и лишняя перезагрузка по loadedKey; на результат не влияет. Тот же принятый класс, что ложный плюс из строкового литерала (спека), просто шире. Оценка: XS (если сузить — вычищать строки/комментарии + брать слова только после FROM/JOIN). Источник: M7b финальный review (линза A).
- [ ] **Ячейка: `onRun={() => commitDraft()}` игнорирует sql-аргумент CM.** Работает (onChange держит draft синхронным), но расходится с explore-паттерном `onRun={run}` — хрупкость при будущих правках SqlEditor. Выровнять: `onRun={(sql) => commitDraft(sql)}`. Оценка: XS. Источник: M7b финальный review (линза A).
- [ ] **PRE-EXISTING: `loadedKey` не видит ре-материализацию таблицы.** Ключ состоит из имён таблиц; `setApplied` (ре-применение схемы) пересоздаёт таблицу с тем же именем → зависимая ячейка держит устаревший результат до ручного ⟳/«выполнить всё». M7b это смягчил (ручной ре-ран появился). Если чинить — включить в ключ поколение/timestamp материализации. Оценка: S. Источник: M7b финальный review (линза A), само поведение из M4.

### Известные-принятые (минор, из финального review M6 — не баги, фиксируем чтоб не потерять)

- [ ] **`AboutModal` без focus-trap.** Оверлей закрывается по «×»/бэкдропу/Esc, но фокус не запирается внутри и не возвращается на «?»-кнопку при закрытии (Tab может уйти под оверлей). a11y-полиш; для одностраничного информационного модала не блокер. Решение: трап + restore-focus (или вынести в общий `Modal`-хелпер, если появится второй). Оценка: S. Источник: M6 финальный review (a11y lens).

- [ ] **Integration-тест примеров: путь не повторяет typing-path UI.** `exampleQueries.integration.test.ts` гоняет SQL рецептов напрямую через DuckDB-клиент, минуя `seedExampleTabs` → редактор → run. Доказывает корректность самих запросов на реальных данных, но не сам акт засева табов в UI. Презентационная грань (TDD-граница CLAUDE.md) — осознанно. При желании паритета: лёгкий тест на `seedTabs`-экшен стора (логика, не презентация). Оценка: XS. Источник: M6 финальный review (test-fidelity lens).

### Известные-принятые (минор, из финального review M7a «Витрины» — не баги, фиксируем чтоб не потерять)

- [ ] **`dropMart` глотает реальные ошибки молча.** `useMartActions.ts` — пустой `catch` оправдан «DROP IF EXISTS идемпотентен», но `IF EXISTS` гасит только «не существует»; ошибка зависимости (удалить витрину, на которую ссылается VIEW другой витрины — путь, который фича сама открывает) бросается, глотается, а `removeDataset` всё равно срабатывает → витрина исчезает из рейла без тоста, объект остаётся в каталоге DuckDB. Несогласованность с `refreshMart` (тот тостит). Фикс (top-кандидат): на пойманной ошибке — тост (как в `refreshMart`), объект всё равно эфемерен → на reload уйдёт. Оценка: XS. Источник: M7a финальный review (code-quality lens).
- [ ] **Reserved-word имена проходят валидацию.** `validateMartName` пропускает `order`/`select`/`table` (совпадают с `^[A-Za-z_][A-Za-z0-9_]*$`); в DDL работают через `quoteIdent`, но заявленная цель валидации («чтоб не нужно было кавычить в ручном SQL/автокомплите») нарушается — `SELECT * FROM order` в другом табе = syntax error. Редкий foot-gun; закрыть denylist'ом при желании. Оценка: XS. Источник: M7a финальный review.
- [ ] **Гонка двойного сабмита → дубликат в сторе.** `createMart` читает `taken` синхронно, потом ждёт `exec`+`describeTable` до `addDataset`; зажатый Enter (key-repeat) может сложить несколько вызовов, все проходят коллизию против устаревшего `taken` → два `<li>` с одним key (React-варнинг). Само-лечится (`dropMart` фильтрует все совпадения), одиночный юзер, низкая вероятность. Фикс: disabled/in-flight guard на кнопках сабмита. Оценка: XS. Источник: M7a финальный review.
- [ ] **Витрина-виджет в отчёте после перезагрузки — вечная ошибка + вводящий в заблуждение хинт.** Пин виджета на `SELECT … FROM my_mart` пишет `my_mart` в `datasetNames`; отчёт персистится в localStorage, витрины — нет. После reload виджет перезапускается и падает навсегда с хинтом «источник(и): my_mart — подгрузи», хотя витрину нельзя подгрузить файлом. Спека осознанно отложила прямой пин витрин и регидрацию — принятое следствие, но острый край. Минимум: где-то сказать юзеру «витрины живут до перезагрузки» (тултип витрины / About). Оценка: S. Источник: M7a финальный review (firewall/spec lens).
- [x] **Пост-Reset утечка каталога → возможная коллизия при загрузке файла.** Закрыто: buildDropDatasetStatements в приёмке M9 — Reset дропает view/table через buildDropMart.
- [ ] **`refreshMart` переставляет витрину в конец списка** (копии stripTrailingSemicolon уже консолидированы в core/sql.ts). Косметика: `removeDataset`+`addDataset` в refresh аппендит → снапшот прыгает вниз «Витрин» (in-place update убрал бы). Оценка: XS. Источник: M7a финальный review.

### Известные-принятые (минор, из финального review терминального редизайна 2026-07-05 — не блокеры)

- [x] **Welcome клипается на низких вьюпортах без скролла.** Закрыт в приёмке M7b (`50a5724`): `overflow-y: auto` на `.welcome-stage`; `margin: auto` у hero при нехватке места схлопывается в 0 — верх достижим. Свирл/вуаль покрывают первый экран, ниже фолда фон — принято. Источник: финальный review терминального редизайна.
- [ ] **Экспортированный отчёт: график бледный на белом.** `plotFigure.ts` хардкодит cyan `#22d3ee` для серий; экспорт рендерит на светлой print-теме → контраст ~1.55:1, бары/линии читаются бледно в HTML/PDF. В приложении (тёмный фон) — ок. Вариант: в export-пути передавать тёмный цвет серий (напр. глубокий teal `#0e7490`). Оценка: S. Источник: финальный review терминального редизайна.

### Известные-принятые (минор, из критического фронт-ревью 2026-07-05 — не блокеры; resize-класс того же ревью закрыт сразу)

- [ ] **Toggle-группы без ARIA-состояния.** `.mode-toggle` (Shell), `.view-toggle` (ResultPanel, WidgetBlockView), `.mart-kind` — активность видна только классом; скринридер не знает, какой вид включён. Фикс: `aria-pressed` (или `role="tablist"`/`aria-selected` для вью-тогглов). Оценка: XS–S. Источник: фронт-ревью 2026-07-05 (M-3).
- [ ] **Report-поверхности mouse-only, без focus ring.** Выбор блока (`Report.tsx` div onClick), редактирование текст-блока (`TextBlockView`), клик по заголовку виджета — нефокусируемы, `:focus-visible`-правило их не покрывает → нет клавиатурного доступа. Продолжение известного a11y-гэпа (dropzone/табы). Оценка: S. Источник: фронт-ревью 2026-07-05 (M-4).
- [ ] **`.topbar` не переносится <~600px.** Для десктоп-инструмента не критично; `flex-wrap: wrap` — дешёвая страховка, но меняет вид топбара на экстремальных ширинах — решить при полишинге. Оценка: XS. Источник: фронт-ревью 2026-07-05 (M-5).
- [ ] **Таб-стрип в 2 ряда ломает иллюзию «приподнятого» активного таба.** При ≥~6 табах стрип враппится; эффект слияния активного таба с карточкой читается только для нижнего ряда. Косметика. Оценка: S (или принять). Источник: фронт-ревью 2026-07-05 (M-7).
- [ ] **`Chart` пересобирает Plot-фигуру на каждый рендер родителя.** `spec` — свежий объект из `buildChartSpec(...)` каждый рендер → эффект с deps `[spec, rows]` перегоняет `plotFigure`+`replaceChildren` на каждый кейстрок поиска/тост. Мемоизировать spec или ключевать эффект примитивами. Смыкается с известным «перерендер на каждый кейстрок». Оценка: XS–S. Источник: фронт-ревью 2026-07-05 (M-8).
- [ ] **DitherSwirl: per-frame `fillStyle`-строки → GC-шум.** Полноэкранный 2D-редроу ~26k ячеек с `toFixed(3)`-строкой на каждую горящую точку; хорошо ограничен (boot/welcome only, пауза на hidden, static при reduced-motion) — не материально. Дешёвый выигрыш при желании: квантовать альфу в ~8 предсобранных fillStyle. Оценка: XS. Источник: фронт-ревью 2026-07-05 (M-9).
- [ ] **Сплит-определение `.grid-th` в двух блоках CSS.** Аддитивно, не конфликтует; слить при следующей чистке index.css. Оценка: XS. Источник: фронт-ревью 2026-07-05 (M-6).

### Известные-принятые (минор, из внешнего ревью 2026-07-02 — не блокеры, фиксируем чтоб не потерять)

- [ ] **`profileResult` профилирует не то, что просят.** Для `paged`-таба переиспользует `_qb_result_<tab>` последнего запуска (аргумент `sql` игнорируется), для raw-таба материализует ТЕКУЩИЙ черновик редактора, который мог не запускаться. Выбрать одну семантику (профиль отображаемого результата) и передавать SQL последнего запуска. Оценка: S.
- [ ] **Explore: двойной fetch окна.** При первом запуске (`view` undefined → DEFAULT_VIEW) и при каждом переключении таба view-эффект перезапрашивает страницу, уже лежащую в сторе. Latest-wins спасает от вреда, но по лишнему запросу на ран/переключение. Guard «view реально поменялся». Оценка: S.
- [ ] **Перерендер на каждый кейстрок.** `updateTabSql` → новый `tabs` → Rail (regex-прогоны pruning), TabStrip, ResultPanel, полный ResultGrid реконсилируются на каждый символ. `React.memo(ResultGrid)` — дешёвый первый шаг. Оценка: XS–S.
- [ ] **Виджет отчёта: мёртвые контролы грида.** `ResultGrid` в отчёте получает no-op `onToggleSort`/`onOpenFilter`, но заголовки кликабельны и ⏷ рисуется. Сделать колбэки опциональными и прятать аффордансы. Оценка: XS.
- [ ] **`Report.tsx` save(): синхронный `revokeObjectURL` после `click()`.** Может оборвать скачивание; `downloadHtml` уже делает отложенный revoke — выровнять. Оценка: XS.
- [ ] **SqlEditor resize: нет `pointercancel`.** После отменённого драга (touch/alt-tab) `pointermove` остаётся на хэндле — ресайз без зажатой кнопки. Добавить `pointercancel` или выход при `ev.buttons === 0`. Оценка: XS.
- [ ] **ProfilePanel: вечное «считаю профиль…» после re-apply.** `setApplied` чистит `ds.profile`, панель показывает спиннер, хотя ничего не считается. Различать «не посчитано» и «считается». Оценка: XS.
- [ ] **Toast: таймер не перезапускается при повторе того же сообщения; нет `role="status"`/`aria-live`.** Оценка: XS.
- [ ] **a11y: dropzone и табы недоступны с клавиатуры.** `CsvDropzone` — кликабельный div без role/tabIndex/key-хендлера; `TabStrip` — div-табы. (+ уже записанный focus-trap `AboutModal`.) Оценка: S.
- [ ] **`exportQuery`: фиксированное имя `qb-export.*` и нет `finally`-очистки.** Двойной экспорт коллидирует; упавший `copyFileToBuffer` течёт виртуальным файлом. Уникальный суффикс + try/finally. Оценка: XS.
- [ ] **TOCTOU-гонка идемпотентности: `Shell.handleFiles`.** Двойной клик/дроп может продублировать датасет (проверка `datasets.some` до async-работы). Module-level in-flight promise / in-flight guard. `loadDemoData`-половина закрыта (M9, inflight Set в `sampleData.ts`). Оценка: XS.
- [ ] **pruning: `a * b` подсвечивает все колонки; ASCII-only токены.** Арифметическая `*` трактуется как `SELECT *`; кириллические/пробельные имена колонок никогда не подсвечиваются. Оценка: S.
- [ ] **`dedupeColumnNames`: ['id','id','id_1'] → второй `id_1` затирает первый.** Вести seen-set по УЖЕ выданным именам. Оценка: XS.
- [ ] **Нет guard на `_qb_`-префикс имён файлов.** Файл `_qb_raw_events.csv` клоберит внутреннюю raw-таблицу `events` и невидим в рейле. Один `isInternalTable`-чек в `loadOneFile` (переименовать с префиксом `f_`, например). Оценка: XS.
- [ ] **Кириллическое имя файла → `________`.** `tableNameFromFilename('продажи.csv')` даёт подчёркивания; fallback `'table'` не срабатывает. Оценка: XS.
- [ ] **`.th-filter.on` не вешается.** CSS-правило индикатора активного фильтра есть, класс в `ResultGrid` не ставится — колонка с фильтром не помечена. Довести или удалить правило. Оценка: XS.
- [ ] **toggleSort-цикл в `ResultPanel` — логика вне core.** asc→desc→remove + additive multi-sort — тестируемая логика в компоненте (TDD-граница CLAUDE.md). Вынести `cycleSort()` в `core/resultQuery.ts` под тесты. Оценка: S.
- [ ] **Инфра-спринт (отдельным решением):** `noUncheckedIndexedAccess` в tsconfig (шумно, но ловит `rows[0]` на пустых результатах); type-aware ESLint (`recommendedTypeChecked` + `no-floating-promises`); CI-workflow на PR/ветках (сейчас гейт только на push в main). Оценка: M.
- [ ] **`stripTrailingSemicolon` снимает только один `;`.** `SELECT 1;;` или `SELECT 1; -- комм` по-прежнему ломают обёртки. Редко; при желании — срезать хвостовые комментарии/повторные `;`. Оценка: XS.

### Известные-принятые (минор, из финального review M5 — не баги, фиксируем чтоб не потерять)

- [x] **`key={block.sql}` на `WidgetBlockView` инертен в Отчёте.** Закрыто: key снят в M7b (внешний div ключуется block.id).

- [ ] **Экспорт: chart-виджет без числовой колонки → рисуется таблицей.** Если у виджета `vizType==='chart'`, но пересчитанный результат не даёт числовой колонки (`buildChartSpec → null`), экспорт показывает таблицу, тогда как живой `WidgetBlockView` рисует пометку «нет числовой колонки для графика». Расхождение live↔export; таблица — разумный фоллбэк, оставлено намеренно. Решить при желании паритета: нести ту же пометку в экспорт. Оценка: XS. Источник: M5 финальный review (integration + code-quality lenses).

## Сделано

### M5 «Экспорт и полировка» (2026-06-26, полишинг-спринт)

- [x] **Dev-консоль: sourcemap-шум DuckDB-WASM** — `customLogger` в `vite.config.ts` глушит ровно строки воркеров DuckDB (`015ba60`).
- [x] **Build chunk-size advisory** — поднят `build.chunkSizeWarningLimit` (`015ba60`).
- [x] **Lint-warning `useVirtualizer`** — точечный `eslint-disable react-hooks/incompatible-library` с обоснованием в `ResultGrid.tsx`; lint теперь 0 errors / 0 warnings (`015ba60`).
- [x] **Неотслеживаемые фикстуры** — `fixtures/acceptance.sql` + `events.csv` закоммичены (`204d582`); `metrics.parquet` — с `!fixtures/metrics.parquet` исключением в `.gitignore`, т.к. `acceptance.sql` на неё ссылается (`5032702`).
- [x] **Метка «применить к колонке» → «применить»** — релейбл в `SchemaColumnEditor` (`42edc89`).
- [x] **Показывать `STRING` вместо `VARCHAR`** — UI-алиас в пикере типов и в схеме рейла; физический тип/DDL остаются VARCHAR (`42edc89`).
- [x] **Мёртвое поле `Dataset.dirty`** — поле + все записи удалены, тесты поправлены (`6f1deb3`).
- [x] **`TextBlockView` пишет на blur всегда** — теперь пишет только при `draft !== block.markdown` (`953fc6f`).
- [x] **Пустой «источник(и):» в ошибке виджета** — при `datasetNames.length === 0` показывается только текст ошибки (`953fc6f`).
- [x] **Рейл в Отчёте при активном текст-блоке** — гасится в Отчёте без активного виджета (`mode==='report'` без widget → `null`); Explore не затронут (`953fc6f`). Приёмка подтвердила — ок. *(Поведение позже осознанно заменено в `70cbf8d`: рейл в отчёте не прячется, а следует активному виджету с fallback на первый источник — см. комментарий в `Rail.tsx`.)*
- [x] **`WidgetBlockView` — сброс в `loading` при смене SQL** — через `key={block.sql}` ремоунт (без синхронного `setState` в эффекте); см. известный-минор выше про инертность в Отчёте (`953fc6f`).
- [x] **Редактирование названий табов** — double-click → inline-инпут; `renameTab(id,title)` (TDD); Enter/blur сохраняет, Esc отменяет (`7a4a693`, `3b8f2c4`).
- [x] **Табы — Sublime-стиль** — перерисован таб-стрип (форма вкладок, выраженный активный, close на ховере, `.tab-rename`) (`36a6e51`); `.tab-title { min-width:0 }` для эллипсиса (`ff420d7`).

### Ранее

- [x] **Dropzone — в рейл, не на всю ширину** (2026-06-26, M4-полиш). Перенесена в рейл над «Источники», компактная; доступна в обоих режимах (удобно для регидрации). (`db21939`)
- [x] **Семантика «сброс»/«сбросить» — унифицировано** (2026-06-23). Оба возвращают к VARCHAR-бейзлайну (`resetColumn` чинён через TDD; per-column «сбросить» = хедерная «сброс» на одну колонку).
